# Versions used to produce the results in the paper.
# Install with:  pip install -r requirements.txt
# scikit-learn in particular is pinned because results can differ across versions.

numpy==1.26.4
pandas==3.0.0
wfdb==4.3.1
neurokit2==0.2.12
scikit-learn==1.2.2
matplotlib==3.8.0
